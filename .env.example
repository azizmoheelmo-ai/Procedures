# Copy to .env and fill in (Supabase dashboard → Project Settings → API).
# The service-role key bypasses RLS — never commit .env.
SUPABASE_URL=https://vqlinmirqycfomtiaynv.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
